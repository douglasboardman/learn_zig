{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug Main",
      "program": "${workspaceFolder}/zig-out/bin/zigversion",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "zig build"
    }
  ]
}
